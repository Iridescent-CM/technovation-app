<% field = ScoreField.new(category, field_name) %>

<%= f.form_group field_name, label: { text: field.label } do %>
  <%= f.check_box field_name, label: field_name.capitalize %>
  <%= f.text_area "#{field_name}_comment", label: 'Comment (optional)' %>
<% end %>
