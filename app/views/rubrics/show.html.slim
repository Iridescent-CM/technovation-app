p DIRECTIONS: The judging rubric for Technovation 2016 contains objective and subjective scoring. The objective score composes of the Ideation, Technical, and Entrepreneurial scores. It is possible for all teams in your scoring batch to receive a perfect score on these objective scoring. The Overall Impression score is where you should score the apps in your batch relative to each other for a subjective score. Maximum points on the subjective part should only be awarded if the submission has truly exhausted their research and development for a complete prototype.

h2 Total score from this judge: #{@rubric.score} / #{Score.total_possible}

= render 'score', category: :ideation, field_name: :identify_problem
= render 'score', category: :ideation, field_name: :address_problem

= render 'score', category: :technical, field_name: :functional
= render 'score', category: :technical, field_name: :external_resources
= render 'score', category: :technical, field_name: :match_features
= render 'score', category: :technical, field_name: :interface

= render 'score', category: :business, field_name: :description
= render 'score', category: :business, field_name: :market
= render 'score', category: :business, field_name: :competition
= render 'score', category: :business, field_name: :revenue
= render 'score', category: :business, field_name: :branding

= render 'score', category: :subjective, field_name: :pitch

= render 'score', category: :bonus, field_name: :launched
