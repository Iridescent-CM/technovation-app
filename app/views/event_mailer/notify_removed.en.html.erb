<tr>
  <td class="content-wrap">
    <meta itemprop="name" content="Removed from event" />
    <table width="100%" cellpadding="0" cellspacing="0">
      <tr>
        <td class="content-block">
          <% if @removed.name.present? %>
            Dear <%= @removed.name %>,
          <% else %>
            Hello,
          <% end %>
        </td>
      </tr>
      <tr>
        <td class="content-block">
          A Chapter Ambassador has just removed your team
          <% if @team_name.present? && @removed.class.to_s == "MentorProfile" %>
            "<%= @team_name %>"
          <% end %>
          from the event:
          <strong><%= @event.name %></strong>
        </td>
      </tr>
      <tr>
        <td class="content-block">
          Send an email to the ambassador who removed you,
          <%= @ambassador_name %>,
          at <%= mail_to @ambassador_email %>
          if you have questions.
        </td>
      </tr>
      <tr>
        <td class="content-block">
          Regards,<br />
          The Technovation Team
        </td>
      </tr>
    </table>
  </td>
</tr>
