<%= simple_form_for(account, url: student_profile_path) do |f| %>
  <%= render 'errors', record: account %>

  <%= f.fields_for :account do |a| %>
    <%= a.input :existing_password,
      placeholder: "Enter your current password here first",
      label: t("views.accounts.edit.existing_password") %>

    <%= a.input :password, label: t("models.account.change_password") %>
  <% end %>

  <p class="hint">
    <%= t("models.account.password_hint") %>
  </p>

  <%= f.button :submit,
    t("views.application.save"),
    class: "button" %>
<% end %>
