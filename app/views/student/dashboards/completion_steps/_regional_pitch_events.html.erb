<div class="panel">
  <h1 class="content-heading">Select a regional pitch event</h1>
  <% unless current_team.selected_regional_pitch_event && current_team.selected_regional_pitch_event.live? %>
    <p>
      Attend a regional pitch event in your area to pitch your submisison to a live panel of judges.
    </p>
      <%= link_to 'Attend an Event',
          student_regional_pitch_events_path,
          class: "button small" %>
  <% else %>
    <p>
      You are attending <%= current_team.selected_regional_pitch_event.name %>.
    </p>
    <div class="step-actions">

          <%= link_to 'view full details',
            [current_scope, current_team.selected_regional_pitch_event],
            class: "button small" %>
    </div>
    <p>
      If this is a mistake or you want to attend a different event please contact your regional ambassador.
    </p>
  <% end %>
</div>