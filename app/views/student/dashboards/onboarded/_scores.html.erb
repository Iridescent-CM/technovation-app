<% if @all_scores.empty? %>
  <%= render 'student/dashboards/onboarded/empty_scores' %>
<% else %>
  <% if @semifinals_scores.any? %>
    <%= render(
      'student/dashboards/onboarded/scores/score_breakdown',
      round: :semifinals,
      round_name: "semifinals",
      scores: @semifinal_scores
    ) %>
  <% end %>

  <% if @quarterfinals_scores.any? %>
    <%= render(
      'student/dashboards/onboarded/scores/score_breakdown',
      round: :quarterfinals,
      round_name: "first round",
      scores: @quarterfinals_scores
    ) %>
  <% end %>
<% end %>

<% if @all_scores.empty? %>
  <%= render 'student/dashboards/info_sidebar', certs: true %>
<% else %>
  <%= render(
    'student/dashboards/onboarded/scores/comments_sidebar'
  ) %>
<% end %>
