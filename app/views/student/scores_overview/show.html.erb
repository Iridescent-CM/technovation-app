<div class="container mx-auto flex flex-col lg:flex-row justify-around gap-6 w-full lg:w-3/4">
  <%= render "student/dashboards/side_nav" %>

  <%= render layout: "application/templates/dashboards/energetic_container", locals: { heading: "Scores & Certificate" } do %>
    <div>
      <% if SeasonToggles.display_scores? %>
        <%= render "slots/student/rebrand/scores" %>
      <% else %>
        <%= render "explanations/feature_not_available", feature: :scores %>
    <% end %>
    </div>
  <% end %>
</div>