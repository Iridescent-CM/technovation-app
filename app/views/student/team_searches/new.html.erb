<div class="team-search">
  <h1 class="appy-title">
    <%= t("views.student.team_searches.new.title") %>
  </h1>
  <p class="appy-subtitle">
    <%= t("views.student.team_searches.new.intro") %>
  </p>

  <div class="search-panel-and-results">
    <div class="search-panel-and-results__search search-filters">
      <div class="search-filters__heading">
        <h4><%= t("views.student.team_searches.new.filter_heading") %></h4>
      </div>

      <div class="search-filters__form-wrapper">
        <%= form_tag request.fullpath, method: :get do %>
          <div class="search-filters__location">
            <%= render 'searches/location_options', account: current_student %>
          </div>
          <div class="search-filters__button-wrapper">
            <%= button_tag t('views.application.filter') %>
          </div>
        <% end %>
      </div>


    </div>

    <div class="search-panel-and-results__results">
      <h2 class="appy-heading-lg">
        <%= t("views.student.team_searches.new.results_heading") %>
      </h2>
      <div class="team-search__teams-wrapper">
        <%= render(partial: "team_searches/result", collection: @teams, cached: true, locals: { account_type: :student }) or
              t("views.student.team_searches.new.no_results") %>
      </div>
      <%= will_paginate @teams %>
    </div>
  </div>
</div>

<!-- kjadsflkj -->

