<%= t("views.student.team_searches.new.intro") %>

<hr />

<%= render(partial: "team_searches/result", collection: @teams, locals: { account_type: :student }) or
      t("views.student.team_searches.new.no_results") %>

<%= will_paginate @teams %>
