<%= link_to t('views.application.dashboard'),
  judge_dashboard_path,
  class: al(judge_dashboard_path) %>

<%= link_to t('views.application.my_profile'),
  judge_profile_path,
  class: al(judge_profile_path) %>

<%= link_to t('views.application.signout'),
  logout_path,
  data: { method: :delete } %>
