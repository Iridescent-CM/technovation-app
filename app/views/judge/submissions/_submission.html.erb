<dl class="submission">
  <dt><%= t('models.submission.team_name') %></dt>
  <dd><%= submission.team_name %></dd>

  <dt><%= t('models.submission.description') %></dt>
  <dd><%= submission.description %></dd>

  <dt><%= t('models.submission.pitch') %></dt>
  <dd><%= submission.pitch %></dd>

  <dt><%= t('models.submission.code') %></dt>
  <dd><%= submission.code %></dd>

  <dt><%= t('models.submission.demo') %></dt>
  <dd><%= submission.demo %></dd>
</dl>
