h1 Scores

- @scores.each do |score|
  h4 Scores for #{score.team}

  - if score.rubrics.any?
    ul
      - score.rubrics.each.with_index do |rubric, index|
        li = link_to "Judge feedback #{index + 1}", rubric
  - else
    p There are no scores available for #{score.team}.
