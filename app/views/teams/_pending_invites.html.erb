<% if invites.any? %>
  <hr />

  <div class="grid">
    <div class="grid__col-auto grid__col--bleed-x">
      <h3 class="reset">Invites sent</h1>

      <% if SeasonToggles.judging_enabled_or_between? %>
        <p class="hint">
          Judging has begun or we are in between judging rounds, so other students
          and mentors will not see these invitations.
        </p>
      <% else %>
        <p class="hint">
          Your team is waiting for these people to accept your invitations.
        </p>
      <% end %>

      <ul class="reset">
        <% invites.each do |invite| %>
          <li class="row">
            <% if invite.invitee %>
              <%= render 'invites/registered_invitee', invite: invite %>
            <% else %>
              <%= render 'invites/unregistered_invitee', invite: invite %>
            <% end %>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
