<%= f.fields_for :account do |a| %>
  <%= a.input :gender,
    label: t('models.account.gender'),
    collection: Account.genders.keys %>
<% end %>

<%= f.input :organization_company_name,
  label: t("models.chapter_ambassador_profile.organization_company_name") %>

<%= f.input :job_title,
  label: t("models.chapter_ambassador_profile.job_title") %>

<%= f.input :bio, label: t("models.chapter_ambassador_profile.bio.signup_label") %>
