<nav class="p-4">
  <ol role="list" class="space-y-6 mb-6 overflow-hidden">
    <%= render "application/templates/side_nav_item",
      name: "Certificates",
      url: send(:"#{current_scope}_certificates_path"),
      is_active_item: al(send(:"#{current_scope}_certificates_path")).present?
    %>
  </ol>
</nav>
