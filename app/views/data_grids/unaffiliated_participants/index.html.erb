<% provide :title, "Unaffiliated Participants" %>

<%= render "datagrid/datagrid",
  grid: @unaffiliated_participants_grid,
  form_url: send("#{current_scope}_unaffiliated_participants_path"),
  model_name: "account",
  scope: current_scope.to_sym
%>
