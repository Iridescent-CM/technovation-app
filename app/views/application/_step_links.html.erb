<% if step.ready? && step.links.any? %>
  <ul>
    <% step.links.each do |link| %>
      <li>
        <%= link.prefix %>
        <%= link_to link.text, link.url(urls), link.options %>
        <%= link.postfix %>
      </li>
    <% end %>
  </ul>
<% end %>
