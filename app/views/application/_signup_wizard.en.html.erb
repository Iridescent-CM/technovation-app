<% previous_attempt ||= OpenStruct.new(id: 0) %>

<%= content_tag
  :div,
  id: "vue-enable-signup-wizard",
  data: {
    previous_attempt: SignupAttemptSerializer.new(previous_attempt)
                        .serialized_json,
  } do %>

  <App />

<% end %>