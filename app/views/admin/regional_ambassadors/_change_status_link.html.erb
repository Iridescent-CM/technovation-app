<%= link_to text, admin_regional_ambassador_path(ambassador, status: status),
                  data: {
                    method: :put,
                    confirm: "Do you want to mark #{ambassador.full_name} as #{status}?",
                  } %>
