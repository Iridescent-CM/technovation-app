<h3>Your location</h3>

<% if current_ambassador.address_details.blank? %>
  <p>
    <%= link_to "Set your location",
      chapter_ambassador_location_details_path(
        return_to: chapter_ambassador_profile_path(anchor: '!location')
      ),
      class: "button" %>
  </p>
<% else %>
  <p><%= current_ambassador.address_details %></p>

  <p class="hint">
    Chapter Ambassadors cannot edit their own location.
    <br />
    Contact <%= mail_to ENV.fetch("HELP_EMAIL") %> if you need to make a change.
  </p>
<% end %>
