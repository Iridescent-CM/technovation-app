  <nav class="p-4">
    <ol role="list" class="space-y-6 mb-6 overflow-hidden">
      <%= render "application/templates/side_nav_item",
         name: "Scores",
         url: mentor_scores_path,
         is_active_item: al(mentor_scores_path).present?
      %>

      <%= render "application/templates/side_nav_item",
         name: "Certificates",
         url: mentor_certificates_path,
         is_active_item: al(mentor_certificates_path).present?
      %>
    </ol>

    <%= render "application/templates/tw_thick_rule" %>

    <div class="tw-tab-content">
      <a id="previous-certificates-tab" href="<%= mentor_certificates_path(anchor: "previous-certificates-tab-content") %>" data-heading="Previous Certificates">Previous Certificates</a>
    </div>
  </nav>
