<%= render 'completion_steps',
           account: current_mentor,
           steps: CompletionSteps.new(current_mentor),
           urls: {
             consent_waiver: {
               view: new_consent_waiver_path(
                 token: current_mentor.consent_token
               ),
             },
             build_profile: {
               bio: edit_mentor_account_path(anchor: 'account-profile-details')
             },
           } %>

<%= render 'team_invitations', invite_type: :mentor, account: current_mentor %>
