<%= t("views.team_searches.new.intro") %>

<%= form_tag request.fullpath, method: :get do %>
  <%= render 'searches/location_options', account: current_mentor %>
  <%= button_tag t("views.application.filter") %>
<% end %>

<hr />

<%= render(partial: "team_searches/result", collection: @teams, cached: true, locals: { account_type: :mentor }) or
      t("views.team_searches.new.no_results") %>

<%= will_paginate @teams %>
