<%= link_to t("views.application.dashboards.menu.dashboard"),
  mentor_dashboard_path,
  class: al(mentor_dashboard_path) %>

<% if current_mentor.onboarded? %>
  <%= link_to t("views.application.dashboards.menu.my_teams"),
    mentor_teams_path,
    class: al(mentor_teams_path) %>

  <%= link_to t("views.application.dashboards.menu.connect_mentors"),
    new_mentor_mentor_search_path,
    class: al(new_mentor_mentor_search_path) %>
<% end %>

<%= link_to t('views.application.my_profile'),
  mentor_profile_path,
  class: al(mentor_profile_path) %>

<%= link_to t('views.application.signout'),
  signout_path,
  data: { method: :delete } %>
